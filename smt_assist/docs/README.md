* `parameters.json` are created by running `z3 -pd` with Z3 4.15.4 and parsing the output.

* `tactics.json` and `probes.json` are created by running `echo "(help-tactic)" | z3 -in` with Z3 4.15.4 and parsing the output.